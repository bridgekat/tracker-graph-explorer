{
  "name": "tracker-viz",
  "version": "1.0.0",
  "private": true,
  "description": "The drawing for `tracker graph`: one HTML file that walks a Lean formalization plan from its areas down to a single theorem.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/publish.mjs",
    "test": "node test/ui.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "marked": "^18.0.12",
    "svelte": "^5.57.0",
    "vite": "^8.2.2",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
